import { off, onlyOff, on, onlyOn } from '../src/events'

describe('on', () => {
  test.todo('___ if port does not exist')
  test.todo('if port exists ____')
  test.todo('sendResponse makes a response ___')
  test.todo('sends message via port.postMessage')
})

describe('off', () => {
  test.todo('removes listener by callback')
})

describe('onlyOn', () => {
  test.todo('gets port ___ try to conect')
  test.todo('sends message')
  test.todo('resolves if no error')
  test.todo('rejects if something throws')
})

describe('onlyOff', () => {
  test.todo('removes listener by callback')
})
