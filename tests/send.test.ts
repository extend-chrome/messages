import { send, onlySend } from '../src/send'

describe('send', () => {
  test.todo('gets port by name')
  test.todo('uses port.sendMessage with message')
  test.todo('port.onMessage listener ')
})

describe('onlySend', () => {
  test.todo('gets port')
  test.todo('sends message')
  test.todo('resolves if no errors')
  test.todo('rejects if something throws')
})
